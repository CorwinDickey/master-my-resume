<ul>
    <a href="#">
        <div>
            <img src="#" alt="#">
            <p><%= currentUser.email %></p>
        </div>
    </a>
    <% for (section of userSections) { %>
        <a href="/section/<%= section._id %>">
            <div class="<% if (section.name === 'Resumes') { %>
                    <%= "resumes-link" %>
                <% } else { %>
                    <%= "section-link" %>
                <% } %>
            ">
            <p><%= section.name %></p>
            </div>
        </a>
    <% } %>
</ul>